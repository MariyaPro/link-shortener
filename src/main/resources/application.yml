spring:
  datasource:
    url: jdbc:postgresql://db:5432/storageLinks_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.postgresqlDialect
    generate-ddl: false
    show-sql: true


servicePrefix: http://sh.l/